# Hot Deal Studio V2 데이터 무결성 & 1:1 렌더링 검증 보고서 (2026-07-30 공식 개정판)

## 📌 개요

본 보고서는 토스쇼핑 무인 핫딜 파이프라인의 **수집(Harvester) ➔ DB 저장 ➔ 홈페이지 렌더링 ➔ 실제 토스 화면** 간의 데이터 불일치 버그 원인을 정밀 추적하고, **5대 항목 1:1 무결성 검증 체계**를 도입한 개정판이다.

> **핵심 교훈:** 수집기 로그 성공(Harvest Success) ≠ 홈페이지 정상 게시(Render Success).
> 5개 필드 중 하나라도 틀리면 게재 실패로 규정한다.

---

# 1. 4단계 데이터 1:1 비교 검증 매트릭스 (Validation Matrix)

단 한 개의 핫딜이라도 게시되기 위해 반드시 아래 4개 레이어에서 데이터가 100% 동일해야 한다.

| 항목 | ① Harvester 수집 | ② DB 저장 | ③ 홈페이지 렌더링 | ④ 실제 토스 화면 | 일치 결과 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **상품명** | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | ❓ 검증 대상 |
| **혜택 가격** | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | ❓ 검증 대상 |
| **할인율** | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | ❓ 검증 대상 |
| **대표 이미지** | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | 100% 매칭 필수 | ❓ 검증 대상 |
| **제휴 쉐어링크** | `toss.im/_m/...` | `toss.im/_m/...` | `toss.im/_m/...` | `toss.im/_m/...` | ❓ 검증 대상 |

---

# 2. 데이터 혼선 및 렌더링 버그 발생 원인 분석

```text
[ 기존 문제 구조 ]

수집기 A (toss_harvester)  ────┐
수집기 B (telegram_watcher) ───┼──> morvix_shop_db.json 병합 (Index / 제목 오매칭 발생)
수집기 C (sharelink_portal) ───┘         │
                                         ▼
                                홈페이지 렌더링 시
                                A의 상품명 + B의 가격 + C의 이미지 + D의 링크 혼선!
```

### 3대 근본 해결책
1. **단일 Primary Key (`product_id`) 기준 병합:** 제목/순서(index) 기반 병합 금지.
2. **5대 필드 검증 게이트 (Validation Gate):**
   - 상품명 / 가격 / 할인율 / 대표 이미지(`resources-fe.toss.im`) / 쉐어링크(`toss.im/_m/`) 중 단 하나라도 누락되면 **DB 저장 및 배포 즉시 차단**.
3. **병렬 수집기 통제:** 단일 파이프라인으로 통합 관리.

---

# 3. 차기 개발 실측 검증 과제 (5대 항목 1:1 추적)

1. **DB 저장 데이터 ➔ 홈페이지 DOM 렌더링 1:1 비교 검증 로그 작성**
2. **실제 토스 앱 화면 ➔ 홈페이지 게재 화면 5대 요소 대조 테스트**
3. **검증 실패 데이터의 자동 배포 차단(Gate Keeper) 테스트**

---

# 결론

"수집 성공" 로그만으로는 파이프라인의 완성을 선언할 수 없다. **실제 홈페이지 렌더링 화면과 토스 실제 화면이 5대 요소를 기준으로 100% 일치할 때 비로소 파이프라인 완성으로 규정**한다.
