services:
  - type: web
    name: morvix-shop
    runtime: python
    region: virginia
    plan: free
    buildCommand: pip install -r requirements.txt && python -m playwright install chromium
    startCommand: python worker/morvix_render_worker.py
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
